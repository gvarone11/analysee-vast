# GPU Inference Service Requirements
# Python 3.11+
#
# NOTE: This service is stateless - no S3 or DB access.
# It receives images via HTTP and returns processed results via HTTP.
# boto3/botocore are intentionally excluded.

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0

# Deep Learning
torch==2.5.1  # PyTorch with CUDA support
torchvision==0.20.1
ultralytics>=8.3.0  # YOLO11 support (C3k2 layer requires >= 8.1)

# Image Processing
opencv-python==4.8.1.78
rfdetr==1.8.1
numpy==1.26.4
Pillow==10.1.0
# AVIF decode for camera images (the R2 upload format); OpenCV cannot decode AVIF
pillow-avif-plugin>=1.5.0

# Geometry
shapely==2.0.2

# Message Queue (RabbitMQ AMQP consumer for inference job queue)
pika>=1.3.0

# Utilities
cachetools==5.3.2
python-dotenv==1.0.0

# Logging/Monitoring
python-json-logger==2.0.7
